# StandX API Credentials
# Copy this file to .env and fill in your actual credentials
# 
# IMPORTANT: Never commit .env to version control!
# The .env file is already in .gitignore

# ============================================
# Required for Trading/Authenticated Endpoints
# ============================================

# JWT Token - Required for authenticated endpoints
# Get this from StandX authentication flow (wallet signature)
# Valid for 7 days by default
STANDX_JWT_TOKEN=your_jwt_token_here

# API Key - Required for trading endpoints (body signature)
# Get this from your StandX account settings
STANDX_API_KEY=your_api_key_here

# API Secret - Required for trading endpoints (body signature)
# Get this from your StandX account settings
# KEEP THIS SECRET - Never expose in client-side code!
STANDX_API_SECRET=your_api_secret_here

# ============================================
# Optional Configuration
# ============================================

# Session ID - Optional, for WebSocket order tracking
# Can be any UUID string, must match between HTTP and WebSocket clients
# If not set, a new UUID will be generated automatically
STANDX_SESSION_ID=your_session_id_here

# Base URL - Optional, defaults to production
# Only change if using a different environment (testnet, staging, etc.)
# STANDX_BASE_URL=https://perps.standx.com

#STANDX onboarded Privatekey for generating authentication and sessions.
STANDX_PRIVATE_KEY=

# ============================================
# Testing Notes
# ============================================
# 
# Public endpoints (query_symbol_price, query_depth_book, etc.) 
# work WITHOUT credentials - you can test those first!
#
# To test without credentials:
#   - Leave all variables empty or don't create .env file
#   - Use: client = StandXClient()  # No credentials needed
#
# To test with credentials:
#   1. Copy this file: cp env.example.template .env
#   2. Fill in your actual credentials
#   3. Run examples: python examples/basic_trading.py

